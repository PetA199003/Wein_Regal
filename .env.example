# Datenbank-Konfiguration
DB_HOST=localhost
DB_USER=wine_admin
DB_PASSWORD=wine_password
DB_NAME=wine_inventory

# Server-Konfiguration
PORT=3000
NODE_ENV=production

# MQTT-Konfiguration (Optional)
# Diese Werte können auch über die Web-UI konfiguriert werden
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_ENABLED=false
